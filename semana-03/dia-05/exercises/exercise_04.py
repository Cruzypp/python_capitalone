"""Exercise: IDs above threshold."""


def ids_above_threshold(records: list[tuple[str, int]], threshold: int) -> list[str]:
    """
    Return the IDs whose amounts exceed the threshold.

    Args:
    records: `(record_id, amount)` tuples.
    threshold: Minimum exclusive threshold.

    Returns:
        IDs with amount greater than threshold.

    Examples:
        Review the test file for representative cases.

    Restrictions:
        Prefer clean interview-ready Python 3.12.

    Edge cases:
        Empty inputs, one-element inputs, duplicates, and basic large cases.

    Target complexity:
        Time O(n), space O(k).

    Interviewer questions:
        What assumptions are you making about the input?
        Can you describe a brute-force alternative first?
    """

    # TODO: Implement the solution.
    raise NotImplementedError
